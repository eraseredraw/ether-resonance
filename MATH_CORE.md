# 🧮 Математическое ядро

## Функция `calc(x, y, time, scale)`

Основная функция расчёта координат. Возвращает `{X, Y}` — экранные координаты.

### 7 взаимосвязанных уравнений

```javascript
// 1. k = x / P.k - 12.5
let k = x / P.k - 12.5;

// 2. e = cos(k) + sin(y / P.e) + cos(k / 2)
let e = Math.cos(k) + Math.sin(y / P.e) + Math.cos(k / 2);

// 3. d = |e|
let d = Math.abs(e);

// 4. q = x / 4 + P.q + d·k·(1 + cos(4d - 2t + y / 72))
let q = x / 4 + P.q + d * k * (1 + Math.cos(4 * d - 2 * time + y / 72));

// 5. c = y·e / 594 - t / 8 + d / 6
let c = y * e / 594 - time / 8 + d / 6;

// 6. X = q·cos(c) + 200
let X = (q * Math.cos(c) + 200) * scale;

// 7. Y = (q/2 + 99·cos(c/2))·sin(c) + 6e + 200
let Y = ((q / 2 + 99 * Math.cos(c / 2)) * Math.sin(c) + 6 * e + 200) * scale;
```

## Параметры формул `P = { k, e, q }`

| Параметр | Диапазон | По умолчанию | Влияние |
|----------|----------|--------------|---------|
| `P.k` | 2–20 | 8 | Делитель X, влияет на частоту волн |
| `P.e` | 5–50 | 24 | Делитель Y, влияет на амплитуду |
| `P.q` | 20–200 | 90 | Добавка к радиусу, влияет на размер |

### Проблема нелинейности ползунков

**Текущая проблема:**
- При движении ползунка от 0→50 — большие изменения
- При движении от 50→100 — почти нет изменений

**Причина:** Параметры входят в формулы нелинейно (в знаменатель, под тригонометрию).

**Решение (варианты):**

1. **Экспоненциальная шкала:**
```javascript
// Вместо линейного P.k = value
P.k = Math.pow(value / 100, 2) * (max - min) + min;
```

2. **Обратная шкала для делителей:**
```javascript
// Для k и e (они в знаменателе)
P.k = maxK / (value / 100);  // 0→100 → maxK→minK
```

3. **Логарифмическая шкала:**
```javascript
P.k = minK * Math.pow(maxK / minK, value / 100);
```

## Сетка координат

```javascript
const GX = 50, GY = 35, STEP = 4;
// Всего точек: (GX*2/STEP + 1) * (GY/STEP + 1) ≈ 900 точек
```

### Кэширование

Точки сетки генерируются один раз в `setup()`:
```javascript
let sidePoints = [];
for (let x = -GX * STEP; x <= GX * STEP; x += STEP) {
    for (let y = 0; y <= GY * STEP; y += STEP) {
        sidePoints.push({ x, y });
    }
}
```

## Организмы

```javascript
organisms = [{
    x: posX,          // Позиция X (пиксели)
    y: posY,          // Позиция Y (пиксели)
    phase: i * 0.5,   // Фаза времени (сдвиг анимации)
    scale: 0.8–1.2    // Индивидуальный размер
}];
```

### Позиции по умолчанию

| # | X (% экрана) | Y (% экрана) |
|---|--------------|--------------|
| 1 | 25% | 50% (лево) |
| 2 | 50% | 50% (центр) |
| 3 | 75% | 50% (право) |
| 4 | 33% | 33% (верх-лево) |
| 5 | 67% | 33% (верх-право) |

## Задачи для Агента 1

- [ ] Добавить 2–3 альтернативные формулы (вариации)
- [ ] Исправить нелинейность ползунков (выбрать метод)
- [ ] Добавить переключатель между формулами
